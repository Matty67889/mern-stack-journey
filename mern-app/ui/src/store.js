/**
 * @fileoverview Global store.
 *
 * Used for for data that is needed for the hierarchy of components
 * that need to be rendered. Created so that parent components of
 * children don't need to know about data if it is not
 * relevant to them (i.e. About page).
 */
const store = {}

export default store;
